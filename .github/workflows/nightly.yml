name: Nightly Test
on:
  schedule:
    - cron: '*/5 * * * *'
    
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
            echo "${{ $github.event_name }}"
